# Full MERN stack tutorial from YT

> Use `npm run start` in `api` folder to start backend
>
> Use `npm run start` in `client` folder to start frontend

<hr>

Built using:

- MongoDB/Mongoose
- Express
- React
- Nodejs

<hr>

**Fully functional**